from setuptools import setup
from setuptools import find_packages

setup(
    name='pytorch_skipgram',
    version='0.0.1',
    description='skip-gram with negative sampling by PyTorch',
    packages=find_packages()
)
